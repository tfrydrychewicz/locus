/** Type declarations for TipTap packages so IDE and tsconfig resolve them. */
declare module '@tiptap/starter-kit' {
  import type { Extension } from '@tiptap/core'
  const StarterKit: Extension
  export default StarterKit
}
